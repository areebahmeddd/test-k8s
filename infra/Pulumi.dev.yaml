config:
  todo-k8s-infra:overlay: dev
  # Secret values are not stored here. Set them with:
  #   pulumi config set --secret todo-k8s-infra:<key> <value>
  # See infra/README.md for the full list of required keys.
