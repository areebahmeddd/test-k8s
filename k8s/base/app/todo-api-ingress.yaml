apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: todo-api
  labels:
    app.kubernetes.io/name: todo-api
    app.kubernetes.io/instance: todo-api
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: todo-k8s
    app.kubernetes.io/managed-by: kustomize
spec:
  ingressClassName: traefik
  # TODO: add a tls block with cert-manager ClusterIssuer annotation in prod.
  # The prod overlay patches the host to the real domain but leaves this HTTP-only.
  rules:
    - host: todo.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: todo-api
                port:
                  number: 8000
